<div class="splash h-screen text-white flex items-center flex-col pt-48 relative">
  <div class="flex flex-col gap-8 items-center">
    <h1 class="text-3xl">My Budget Mate</h1>
    <%= image_tag 'budget-app.gif', size: "100x100", alt: "logo"%>
  </div>
  <% if user_signed_in? %>
    <p class="absolute top-4 right-4">Welcome <%= current_user.name %> 😃</p>
    <div class="flex gap-6 mt-12">
        <button class="btn p-4 rounded-lg">
          <%= link_to 'Categories', categories_path %>
        </button>
    <%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: "logout p-4 rounded-lg" %>
    </div>
  <% else %>
    <%= link_to 'Log In', new_user_session_path, class: "border border-solid rounded-lg p-2 mb-4" %>
    <%= link_to 'Sign up', new_user_registration_path, class: "border border-solid rounded-lg p-2 mb-4" %>
  <% end %>
</div>